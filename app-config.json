{
  "command": "npm run start",
  "buildPath": "build",
  "stack": "node20",
  "env_variables": {},
  "memory": 256,
  "scripts": {
    "preserve": "cd client && npm install && npm run build && npm run pack",
    "predeploy": "cd client && npm install && npm run build && npm run pack"
  }
}
